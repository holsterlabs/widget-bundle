{% extends '@Widget/_layouts/widget.html.twig' %}

{%- block widget -%}
    {%- set content = content ?? '' -%}
    <div class="widget-content">
        {%- if text_html is defined and text_html is same as(true) -%}
            {{- text|raw -}}
        {%- else -%}
            {{- text -}}
        {%- endif -%}
    </div>
{%- endblock widget -%}
