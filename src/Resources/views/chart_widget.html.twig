{% extends '@Widget/_layouts/widget.html.twig' %}

{%- block widget -%}
    {%- set content = content ?? '' -%}
    <div class="widget-chart" data-chart="{{ data|json_encode()|e }}">
        <canvas></canvas>
    </div>
{%- endblock widget -%}
